-- scripts/core/roboport_repair_pack_logistics_0715.lua
-- Tech Priests 0.1.674-dev recovery.
-- Canonical repair-pack service for existing roboports. Discovery is broker-owned;
-- classification is read-only; single_dispatcher_0510 alone executes transactions.
-- Defensive and construction planners remain the sole placement authority.

local M={
 version="0.1.674-dev",storage_key="roboport_repair_pack_logistics_0715",
 pickup_reach_sq=2.56,target_reach_sq=3.24,return_reach_sq=2.56,
 move_priority=973,move_ttl=60*10,reservation_ttl=60*15,request_timeout=60*14,
 discovery_interval=227,max_pairs_per_discovery=6,max_transfer=20,
 dispatcher_owned=true,discovery_only_broker=true,robot_inventory_excluded=true,
 placement_authority=false,
}
local REPAIR_PREFERENCE={"repair-pack"}
local function now()return game and game.tick or 0 end
local function valid(e)return e and e.valid end
local function safe(v)local ok,s=pcall(tostring,v);return ok and s or"?"end
local function valid_pair(p)return type(p)=="table"and valid(p.station)and valid(p.priest)end
local function station_unit(p)return p and(p.station_unit or(valid(p.station)and p.station.unit_number))or nil end
local function pair_map()return storage and storage.tech_priests and storage.tech_priests.pairs_by_station or{}end
local function dist_sq(a,b)if not(a and b)then return 1e18 end;local x=(a.x or 0)-(b.x or 0);local y=(a.y or 0)-(b.y or 0);return x*x+y*y end
function M.root()storage.tech_priests=storage.tech_priests or{};local r=storage.tech_priests[M.storage_key]or{version=M.version,enabled=true,stats={},recent={},cursor=0};storage.tech_priests[M.storage_key]=r;r.version=M.version;if r.enabled==nil then r.enabled=true end;r.dispatcher_owned=true;r.discovery_only_broker=true;r.robot_inventory_excluded=true;r.placement_authority=false;r.stats=r.stats or{};r.recent=r.recent or{};r.cursor=tonumber(r.cursor)or 0;return r end
local function stat(k,n)local r=M.root();r.stats[k]=(tonumber(r.stats[k])or 0)+(tonumber(n)or 1)end
local function record(pair,a,d)local r=M.root();local e={tick=now(),action=tostring(a),station=safe(station_unit(pair)),detail=tostring(d or"")};r.recent[#r.recent+1]=e;while#r.recent>160 do table.remove(r.recent,1)end;if pair then pair.roboport_repair_logistics_last_0715=e end end
local function count(i,n)if not(i and i.valid and n)then return 0 end;local ok,v=pcall(i.get_item_count,i,n);return ok and(tonumber(v)or 0)or 0 end
local function remove(i,n,c)if not(i and i.valid and n and(c or 0)>0)then return 0 end;local ok,v=pcall(i.remove,i,{name=n,count=c});return ok and(tonumber(v)or 0)or 0 end
local function insert(i,n,c)if not(i and i.valid and n and(c or 0)>0)then return 0 end;local ok,v=pcall(i.insert,i,{name=n,count=c});return ok and(tonumber(v)or 0)or 0 end
local function can_insert(i,n)if not(i and i.valid and n)then return false end;local ok,v=pcall(i.can_insert,i,{name=n,count=1});return ok and v==true end
local function item_type(n)local p=prototypes and prototypes.item and prototypes.item[n];local t;if p then pcall(function()t=p.type end)end;return t end
local function readiness()return rawget(_G,"TechPriestsRoboportReadiness0714")or package.loaded["scripts.core.roboport_readiness_0714"]end
local function storage_authority()return rawget(_G,"TechPriestsStorageRoleAuthority0686")or package.loaded["scripts.core.storage_role_authority_0686"]end
local function reservations()local r=rawget(_G,"TechPriestsWorkReservations0601")or package.loaded["scripts.core.work_reservations"];if not r then local ok,m=pcall(require,"scripts.core.work_reservations");if ok then r=m end end;return r end
local function ensure_category()local r=reservations();if not r then return nil end;local category="roboport-repair-pack-logistics";local found=false;for _,v in ipairs(r.categories or{})do if v==category then found=true break end end;if not found then r.categories=r.categories or{};r.categories[#r.categories+1]=category end;local state=type(r.root)=="function"and r.root()or nil;if state then state.reservations=state.reservations or{};state.reservations[category]=state.reservations[category]or{}end;return r end
local function claim(pair,task)local r=ensure_category();if not(r and type(r.claim)=="function"and valid(task.target))then return false,"reservation-unavailable"end;local ok,why=r.claim("roboport-repair-pack-logistics",task.target,pair,M.reservation_ttl,{surface_index=pair.station.surface.index,force_index=pair.station.force.index,family="roboport-repair-pack",item=task.item,source="roboport-0715"});task.reserved_0715=ok==true;return ok==true,why end
local function release(pair,task)if not(task and valid(task.target))then return false end;local r=reservations();if r and type(r.release)=="function"then local ok,v=pcall(r.release,"roboport-repair-pack-logistics",task.target,pair);return ok and v==true end;return false end
local function move(pair,target,reason)if not(valid_pair(pair)and valid(target))then return false end;local f=rawget(_G,"tech_priests_request_movement_0418");if type(f)~="function"then return false end;local ok,accepted=pcall(f,pair,target.position,reason,{owner="roboport-repair-pack-logistics-0715",priority=M.move_priority,ttl=M.move_ttl,radius=1.5,distraction=defines and defines.distraction and defines.distraction.none or nil});return ok and accepted==true end
local function radius(pair)local v=tonumber(pair.radius)or 28;if type(_G.get_station_operating_radius)=="function"then local ok,n=pcall(_G.get_station_operating_radius,pair.station);if ok and tonumber(n)then v=tonumber(n)end end;return math.max(8,math.min(v,96))end
local function sources(pair)local out,seen={},{};local home=station_unit(pair);local function add(s)local i,e=s and s.inv,s and s.entity;if not(i and i.valid and valid(e)and e.surface==pair.station.surface and e.force==pair.station.force)then return end;if s.authority_source_station_0573 and tostring(s.authority_source_station_0573)~=tostring(home)then return end;if dist_sq(e.position,pair.station.position)>radius(pair)^2 then return end;local k=safe(i);if seen[k]then return end;seen[k]=true;out[#out+1]={inv=i,entity=e,label=s.source or s.inventory_id or"home-source"}end;local f=rawget(_G,"tech_priests_inventory_steward_sources_for_pair");if type(f)=="function"then local ok,t=pcall(f,pair);if ok and type(t)=="table"then for _,s in ipairs(t)do add(s)end end end;local ids=defines and defines.inventory;if ids and pair.station.get_inventory then local ok,i=pcall(pair.station.get_inventory,pair.station,ids.chest);if ok and i and i.valid then add{inv=i,entity=pair.station,source="station-chest"}end end;return out end
local function source_for(pair,item,target_inv)local best;for _,s in ipairs(sources(pair))do local n=count(s.inv,item);if n>0 and can_insert(target_inv,item)then local score=dist_sq(pair.priest.position,s.entity.position)-math.min(n,100);if not best or score<best.score then best={item=item,count=n,inv=s.inv,entity=s.entity,label=s.label,score=score}end end end;return best end
local function choose(pair,report)local target=report and report.material_inventory;if not(target and target.valid)then return nil,nil end;for _,x in ipairs(report.materials and report.materials.entries or{})do if item_type(x.name)=="repair-tool"then local s=source_for(pair,x.name,target);if s then return s,x.name end end end;for _,n in ipairs(REPAIR_PREFERENCE)do local s=source_for(pair,n,target);if s then return s,n end end;for _,n in ipairs(report.compatible_repair_packs or{})do local s=source_for(pair,n,target);if s then return s,n end end;return nil,(report.compatible_repair_packs or{})[1]or"repair-pack" end
local function refresh(pair)local r=readiness();if r and type(r.scan_pair)=="function"then pcall(r.scan_pair,pair,true)end;return pair.roboport_reports_0714 or{}end
local function eligible(report)return type(report)=="table"and valid(report.entity)and report.state=="manual-repair-pack-service-eligible"and report.connected_item_automation~=true and report.energy and report.energy.connected==true and report.network and report.network.present==true and report.material_inventory and report.material_inventory.valid end
function M.discover_pair(pair,force)
 if M.root().enabled==false or not valid_pair(pair)then return nil end;local active=pair.roboport_repair_logistics_0715 or pair.roboport_repair_custody_0715;if active then return pair.roboport_repair_candidate_0715 end;local best;for _,report in ipairs(refresh(pair))do if eligible(report)then local src,item=choose(pair,report);local missing=math.max(1,tonumber(report.repair_pack_missing)or 1);local score=dist_sq(pair.priest.position,report.entity.position)-math.min(missing,100);if not best or score<best.score then best={version=M.version,tick=now(),target=report.entity,target_unit=report.entity.unit_number,target_name=report.entity.name,item=item,source=src,missing=missing,score=score,reason=src and"source-ready"or"waiting-source"}end end end;pair.roboport_repair_candidate_0715=best;return best
end
function M.recommend_action(pair)local task=pair and pair.roboport_repair_logistics_0715;local custody=pair and pair.roboport_repair_custody_0715;local candidate=pair and pair.roboport_repair_candidate_0715;local v=task or custody or candidate;if not v then return nil end;return{kind="roboport-repair-pack-logistics",family="roboport-repair-pack-logistics",target=v.target,position=valid(v.target)and v.target.position or nil,item=v.item,phase=v.phase or(task and task.phase)or"candidate",source="roboport-repair-pack-logistics-0715",reason=v.reason or"repair-pack-service",active=task~=nil or custody~=nil}end
local function sync(pair,task,reason)local c=task and task.carried;if c and c.item and(tonumber(c.count)or 0)>0 then pair.roboport_repair_custody_0715={version=M.version,tick=now(),phase="removed-not-credited",item=c.item,count=c.count,target=task.target,target_unit=task.target_unit,target_name=task.target_name,source_entity=task.source_entity,source_inv=task.source_inv,source_label=task.source_label,reason=reason or task.phase};return true end;pair.roboport_repair_custody_0715=nil;return false end
local function finish(pair,task,reason)release(pair,task);pair.roboport_repair_custody_0715=nil;pair.roboport_repair_candidate_0715=nil;pair.roboport_repair_logistics_last_task_0715=task;pair.roboport_repair_logistics_0715=nil;record(pair,"finished",reason);return{processed=1,acted=1,detail=reason}end
local function deposit(pair,item,n)local a=storage_authority();if a and type(a.deposit_exact)=="function"then return a.deposit_exact(pair,item,n,"roboport-repair-return-0715",{})end;return false,"storage-unavailable",0 end
local function return_custody(pair,task)local c=task.carried;if not(c and(c.count or 0)>0)then return finish(pair,task,"empty-custody")end;local target=valid(task.source_entity)and task.source_entity or pair.station;if dist_sq(pair.priest.position,target.position)>M.return_reach_sq then task.phase="return-custody";sync(pair,task,"returning");if not move(pair,target,"roboport-repair-return-0715")then return{processed=1,blocked=1,detail="return-movement-blocked"}end;return{processed=1,waiting=1,detail="returning-custody"}end;if valid(task.source_entity)and task.source_inv and task.source_inv.valid then local n=insert(task.source_inv,c.item,c.count);c.count=c.count-n;if c.count<=0 then return finish(pair,task,"custody-returned-source")end;task.source_entity=nil;task.source_inv=nil;sync(pair,task,"source-return-partial")end;local ok,why,n=deposit(pair,c.item,c.count);n=tonumber(n)or 0;c.count=c.count-n;if ok==true and c.count<=0 then return finish(pair,task,"custody-stored")end;sync(pair,task,"storage-blocked");return{processed=1,blocked=1,detail="storage-blocked:"..safe(why)}end
local function begin(pair,c)local task={version=M.version,phase="waiting-source",target=c.target,target_unit=c.target_unit,target_name=c.target_name,item=c.item,count=math.max(1,math.min(M.max_transfer,c.missing or 1)),started_tick=now(),reason=c.reason};local ok,why=claim(pair,task);if not ok then return nil,why end;pair.roboport_repair_logistics_0715=task;return task end
local function target_report(pair,task)local r=readiness();if r and type(r.inspect_entity)=="function"then local ok,q=pcall(r.inspect_entity,pair,task.target,true);if ok then return q end end;return nil end
function M.abort_pair(pair,reason)local task=pair and pair.roboport_repair_logistics_0715;if not task then return{processed=0,detail="no-task"}end;if sync(pair,task,reason)then task.phase="return-custody";return return_custody(pair,task)end;release(pair,task);pair.roboport_repair_logistics_0715=nil;pair.roboport_repair_candidate_0715=nil;return{processed=1,blocked=1,detail=reason or"aborted"}end
function M.service_pair(pair,reason)
 if M.root().enabled==false or not valid_pair(pair)then return{processed=0,failed=not valid_pair(pair)and 1 or 0,detail="disabled-or-invalid"}end;if pair.roboport_repair_custody_0715 and not pair.roboport_repair_logistics_0715 then local c=pair.roboport_repair_custody_0715;pair.roboport_repair_logistics_0715={version=M.version,phase="return-custody",item=c.item,count=c.count,carried={item=c.item,count=c.count},target=c.target,target_unit=c.target_unit,target_name=c.target_name,source_entity=c.source_entity,source_inv=c.source_inv,source_label=c.source_label,started_tick=now(),reason="orphan-custody"}end;local task=pair.roboport_repair_logistics_0715;if not task then local c=pair.roboport_repair_candidate_0715 or M.discover_pair(pair,true);if not c then return{processed=1,waiting=1,detail="no-candidate"}end;local why;task,why=begin(pair,c);if not task then return{processed=1,blocked=1,detail="reservation-blocked:"..safe(why)}end end
 if task.phase=="return-custody"then return return_custody(pair,task)end;if not valid(task.target)then return M.abort_pair(pair,"target-invalid")end;local report=target_report(pair,task);if not eligible(report)then return M.abort_pair(pair,"target-no-longer-eligible")end
 if task.phase=="waiting-source"then local src=source_for(pair,task.item,report.material_inventory);if not src then if now()-task.started_tick>M.request_timeout then return M.abort_pair(pair,"source-timeout")end;return{processed=1,waiting=1,detail="waiting-source"}end;task.source_entity=src.entity;task.source_inv=src.inv;task.source_label=src.label;task.phase="move-source" end
 if task.phase=="move-source"then if dist_sq(pair.priest.position,task.source_entity.position)>M.pickup_reach_sq then if not move(pair,task.source_entity,"roboport-repair-source-0715")then return{processed=1,blocked=1,detail="source-movement-blocked"}end;return{processed=1,waiting=1,detail="moving-source"}end;task.phase="pickup"end
 if task.phase=="pickup"then local n=remove(task.source_inv,task.item,task.count);if n<=0 then return M.abort_pair(pair,"source-remove-failed")end;task.carried={item=task.item,count=n};sync(pair,task,"removed-source");task.phase="move-target"end
 if task.phase=="move-target"then if dist_sq(pair.priest.position,task.target.position)>M.target_reach_sq then if not move(pair,task.target,"roboport-repair-target-0715")then task.phase="return-custody";return return_custody(pair,task)end;return{processed=1,waiting=1,detail="moving-target"}end;task.phase="deliver"end
 if task.phase=="deliver"then report=target_report(pair,task);if not eligible(report)then task.phase="return-custody";return return_custody(pair,task)end;local c=task.carried;if not c then return M.abort_pair(pair,"custody-missing")end;local n=insert(report.material_inventory,c.item,c.count);c.count=c.count-n;if n>0 then stat("repair-packs-delivered",n)end;if c.count<=0 then return finish(pair,task,"repair-packs-delivered")end;task.phase="return-custody";sync(pair,task,"partial-delivery");return return_custody(pair,task)end
 return{processed=1,failed=1,detail="unknown-phase:"..safe(task.phase)}
end
local function register_service()local b=rawget(_G,"TechPriestsRuntimeTickBroker0600");if not(b and type(b.register_service)=="function")then return false end;local service=b.register_service{name="roboport_repair_pack_discovery_0715",category="machine-logistics",interval=M.discovery_interval,priority=79,budget=M.max_pairs_per_discovery,note="discovery only; dispatcher owns existing-roboport repair-pack service",fn=function(_,budget)local list={};for k,p in pairs(pair_map())do if valid_pair(p)then list[#list+1]={key=tostring(k),pair=p}end end;table.sort(list,function(a,c)return a.key<c.key end);local r=M.root();local limit=math.min(#list,math.max(0,math.floor(tonumber(budget)or M.max_pairs_per_discovery)));if limit==0 then return{processed=0,acted=0,detail="no-pairs"}end;local start=r.cursor%#list+1;for i=0,limit-1 do pcall(M.discover_pair,list[((start+i-1)%#list)+1].pair,false)end;r.cursor=(start+limit-2)%#list+1;return{processed=limit,acted=0,exhausted=#list>limit,detail="pairs="..limit}end};return service~=nil end
function M.install()M.root();ensure_category();_G.TechPriestsRoboportRepairPackLogistics0715=M;if not register_service()then return false end;if log then log("[Tech-Priests recovery] dispatcher-owned roboport repair-pack logistics armed; placement and robot population remain external")end;return true end
return M
