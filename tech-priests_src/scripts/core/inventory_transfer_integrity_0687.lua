-- scripts/core/inventory_transfer_integrity_0687.lua
-- Retired by Milestone 0808. Persistent priest-cargo custody is consolidated
-- directly into scripts.core.inventory_steward. This file is source-preserved
-- for history only and may not install, register routes, patch modules, or mutate state.

local M = {
  version = "0.1.674-dev",
  retired = true,
  reason = "persistent priest cargo custody consolidated into inventory_steward",
}

return M
