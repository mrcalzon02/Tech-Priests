name: Archived experimental prerelease RC3

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  archived-experimental-prerelease:
    runs-on: ubuntu-latest
    steps:
      - name: Refuse historical RC3 publication
        run: |
          echo "v0.1.674-rc.3 is an archived experimental prerelease workflow." >&2
          echo "Verified publication requires recovery Gates 1-5, VERIFIED_RELEASE_AUTHORIZATION.json, and the canonical packager." >&2
          exit 1
