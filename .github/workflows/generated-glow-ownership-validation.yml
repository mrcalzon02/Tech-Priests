name: Generated glow ownership validation

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read
  statuses: write

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v4
        with:
          show-progress: false

      - name: Audit canonical generated glow ownership
        shell: bash
        run: |
          set -o pipefail
          python3 tools/check_generated_glow_ownership_0781.py 2>&1 | tee /tmp/generated-glow-ownership.log

      - name: Retain generated glow ownership failure diagnostics
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: generated-glow-ownership-output
          path: /tmp/generated-glow-ownership.log
          if-no-files-found: ignore
          retention-days: 7

      - name: Publish generated glow ownership validation status
        if: always() && github.event_name == 'push'
        uses: actions/github-script@v7
        with:
          script: |
            const passed = '${{ job.status }}' === 'success';
            await github.rest.repos.createCommitStatus({
              owner: context.repo.owner,
              repo: context.repo.repo,
              sha: context.sha,
              state: passed ? 'success' : 'failure',
              context: 'tech-priests/generated-glow-ownership',
              description: passed ? 'Canonical generated glow ownership passed' : 'Canonical generated glow ownership failed',
              target_url: `${context.serverUrl}/${context.repo.owner}/${context.repo.repo}/actions/runs/${context.runId}`,
            });
