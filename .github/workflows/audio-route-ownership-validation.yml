name: Audio route ownership validation

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read
  statuses: write

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v4
        with:
          show-progress: false

      - name: Audit audio route ownership
        shell: bash
        run: |
          set -o pipefail
          python3 tools/check_audio_route_ownership_0803.py 2>&1 | tee /tmp/audio-route-ownership.log

      - name: Retain audio route ownership failure diagnostics
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: audio-route-ownership-output
          path: /tmp/audio-route-ownership.log
          if-no-files-found: ignore
          retention-days: 7

      - name: Publish audio route ownership status
        if: always() && github.event_name == 'push'
        uses: actions/github-script@v7
        with:
          script: |
            const passed = '${{ job.status }}' === 'success';
            await github.rest.repos.createCommitStatus({
              owner: context.repo.owner,
              repo: context.repo.repo,
              sha: context.sha,
              state: passed ? 'success' : 'failure',
              context: 'tech-priests/audio-route-ownership',
              description: passed ? 'Audio route ownership passed' : 'Audio route ownership failed',
              target_url: `${context.serverUrl}/${context.repo.owner}/${context.repo.repo}/actions/runs/${context.runId}`,
            });