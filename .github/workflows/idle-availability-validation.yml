name: Idle availability ownership validation

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read
  statuses: write

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v4
        with:
          show-progress: false

      - name: Audit canonical idle availability predicate ownership
        shell: bash
        run: |
          set -o pipefail
          python3 tools/check_idle_availability_ownership_0787.py 2>&1 | tee /tmp/idle-availability-ownership.log

      - name: Retain idle availability failure diagnostics
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: idle-availability-output
          path: /tmp/idle-availability-ownership.log
          if-no-files-found: ignore
          retention-days: 7

      - name: Publish idle availability validation status
        if: always() && github.event_name == 'push'
        uses: actions/github-script@v7
        with:
          script: |
            const passed = '${{ job.status }}' === 'success';
            await github.rest.repos.createCommitStatus({
              owner: context.repo.owner,
              repo: context.repo.repo,
              sha: context.sha,
              state: passed ? 'success' : 'failure',
              context: 'tech-priests/idle-availability-ownership',
              description: passed ? 'Idle availability ownership passed' : 'Idle availability ownership failed',
              target_url: `${context.serverUrl}/${context.repo.owner}/${context.repo.repo}/actions/runs/${context.runId}`,
            });
