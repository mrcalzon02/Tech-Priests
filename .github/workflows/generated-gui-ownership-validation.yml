name: Generated GUI ownership validation

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: write
  statuses: write

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          show-progress: false

      - name: Apply guarded 0784 beam and extraction consolidation
        if: github.event_name == 'push'
        shell: bash
        run: |
          set -euo pipefail
          trigger='.github/triggers/consolidate-generated-beam-0784.trigger'
          source='tech-priests_src/scripts/generated/control_legacy_part_022.lua'
          if [[ ! -f "${trigger}" ]]; then
            echo '0784 trigger is absent; source remains unchanged.'
            exit 0
          fi
          if grep -q 'TECH_PRIESTS_0315_DIRECT_SERVICE_OVERRIDE_RETIRED = true' "${source}"; then
            echo '0784 source transformation is already present.'
            exit 0
          fi
          python3 .github/scripts/consolidate_generated_beam_0784_temp.py 2>&1 | tee /tmp/generated-beam-0784.log
          git diff --check
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add \
            tech-priests_src/scripts/generated/control_legacy_part_005.lua \
            tech-priests_src/scripts/generated/control_legacy_part_008.lua \
            tech-priests_src/scripts/generated/control_legacy_part_021.lua \
            tech-priests_src/scripts/generated/control_legacy_part_022.lua \
            tech-priests_src/scripts/core/combat_safety.lua \
            tech-priests_src/scripts/core/runtime_command_cleanup_0720.lua \
            docs/DEVELOPMENT_HISTORY.md
          if git diff --cached --quiet; then
            echo '0784 produced no source changes.' >&2
            exit 1
          fi
          git commit -m 'Consolidate generated mining beam and extraction ownership'
          git push origin HEAD:main

      - name: Audit canonical generated GUI ownership
        shell: bash
        run: |
          set -o pipefail
          python3 tools/check_generated_gui_ownership_0783.py 2>&1 | tee /tmp/generated-gui-ownership.log

      - name: Retain generated GUI ownership failure diagnostics
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: generated-gui-ownership-output
          path: |
            /tmp/generated-beam-0784.log
            /tmp/generated-gui-ownership.log
          if-no-files-found: ignore
          retention-days: 7

      - name: Publish generated GUI ownership validation status
        if: always() && github.event_name == 'push'
        uses: actions/github-script@v7
        with:
          script: |
            const passed = '${{ job.status }}' === 'success';
            await github.rest.repos.createCommitStatus({
              owner: context.repo.owner,
              repo: context.repo.repo,
              sha: context.sha,
              state: passed ? 'success' : 'failure',
              context: 'tech-priests/generated-gui-ownership',
              description: passed ? 'Canonical generated GUI ownership passed' : 'Canonical generated GUI ownership failed',
              target_url: `${context.serverUrl}/${context.repo.owner}/${context.repo.repo}/actions/runs/${context.runId}`,
            });
