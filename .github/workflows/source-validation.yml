name: Apply 0503 missing-priest recovery narrowing

on:
  push:
    branches:
      - main

permissions:
  contents: write
  statuses: write

jobs:
  repair:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          show-progress: false

      - name: Apply one-shot missing recovery lease contract
        shell: bash
        run: |
          set -euo pipefail
          test -f tools/apply_0503_missing_recovery_narrowing_temp.py
          python3 tools/apply_0503_missing_recovery_narrowing_temp.py
          git config user.name "Tech Priests Recovery"
          git config user.email "actions@users.noreply.github.com"
          git add -A
          git commit -m "Narrow 0503 to one-shot broker missing-priest recovery"
          git pull --rebase origin main
          git push origin HEAD:main

      - name: Publish repair status
        if: always()
        uses: actions/github-script@v7
        with:
          script: |
            const passed = '${{ job.status }}' === 'success';
            await github.rest.repos.createCommitStatus({
              owner: context.repo.owner,
              repo: context.repo.repo,
              sha: context.sha,
              state: passed ? 'success' : 'failure',
              context: 'tech-priests/0503-narrowing',
              description: passed ? '0503 missing recovery narrowing commit pushed' : '0503 missing recovery narrowing failed',
              target_url: `${context.serverUrl}/${context.repo.owner}/${context.repo.repo}/actions/runs/${context.runId}`,
            });
