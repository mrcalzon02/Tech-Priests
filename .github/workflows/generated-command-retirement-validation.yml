name: Generated command retirement validation

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read
  statuses: write

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v4
        with:
          show-progress: false

      - name: Inspect remaining generated command ownership
        shell: bash
        run: python3 .github/scripts/inspect_remaining_generated_commands_0793_temp.py

      - name: Retain remaining generated command inventory
        uses: actions/upload-artifact@v4
        with:
          name: remaining-generated-command-ownership-0793
          path: /tmp/remaining-generated-command-ownership-0793.json
          if-no-files-found: error
          retention-days: 7

      - name: Publish generated command retirement validation status
        if: always() && github.event_name == 'push'
        uses: actions/github-script@v7
        with:
          script: |
            const passed = '${{ job.status }}' === 'success';
            await github.rest.repos.createCommitStatus({
              owner: context.repo.owner,
              repo: context.repo.repo,
              sha: context.sha,
              state: passed ? 'success' : 'failure',
              context: 'tech-priests/generated-command-retirement',
              description: passed ? 'Temporary remaining generated command inspection passed' : 'Temporary remaining generated command inspection failed',
              target_url: `${context.serverUrl}/${context.repo.owner}/${context.repo.repo}/actions/runs/${context.runId}`,
            });
